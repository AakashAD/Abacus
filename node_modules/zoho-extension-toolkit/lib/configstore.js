'use strict';

//Config file is stored in /Users/eugine-9780/Library/Preferences/zoho-extension-toolkit-nodejs
const pkg = require('../package.json');

const Configstore = new (require('conf'))({ configName: pkg.name, projectName: pkg.name });

module.exports = Configstore;
