/*
Copyright (c) 2017, ZOHO CORPORATION
License: MIT
*/

var path = require('path');
var { writeJsonSync } = require('fs-extra');

module.exports = (jsonDir, key, value) => {
  var json = require(jsonDir);
  json[key] = value;
  writeJsonSync(jsonDir, json, { spaces: '\t' });
  return true;
};
